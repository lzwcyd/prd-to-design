# 系分模版

# 1、概述

`需求链接`

`技术相关的架构变化`

**核心结论：** _（一句话说清本期改动的性质与影响面，例："增量式接入，零破坏性改动"；让人一眼抓到重点）_

**改动量一览：**

| 工程 | 改动量 | 改动内容 |
| --- | --- | --- |
| {工程A} | 小 / 中 / 大 | {改了什么} |

**业务术语 ↔ 代码术语（带证据）：**

| 业务术语 | 代码术语 | 证据（类 / 方法 / 文件:行） |
| --- | --- | --- |
| {业务词} | {代码符号} | {ClassName#method / path:line} |

# 2、方案设计

## 2.1、业务用例

_用例图_

## 2.2、系统组件

_组件图_

_关键模块改动清单（贴代码，显式标注"不动"的关键链路）：_

| 工程 | 文件 / 模块 | 改动类型 | 说明 |
| --- | --- | --- | --- |
| {工程} | {path/Class} | 新增 / 改造 / 不动 | {说明} |

## 2.3、交互时序

> 图例约定：时序 / 组件图用加粗或 Mermaid `classDef`/`style` **高亮本期新增/变更**的步骤与节点，并在图旁注明一行图例（如"加粗 = 本期新增"），让读者聚焦改动点，而非均匀平铺整条链路。

_时序图_

# 3、数据库设计

## 3.1、数据模型图

_ER图_

## 3.2、数据模型验证

_不同表之间关键数据关系验证逻辑_

## 3.3、重要字段生成规则

_如：单号、费用、流水号_

## 3.4、表结构设计

_创建 / 变更语句（DDL 带注释 + 回滚；无表变更则写"本需求无表结构变更"）：_

```sql
-- 系统 / 库 / 表：{system} / {db} / {table}
-- 变更：{为什么改}
-- 兼容：{历史行如何兼容，如 NULL=不启用=与上线前行为完全一致}
ALTER TABLE {table}
    ADD COLUMN {col} {type} NULL DEFAULT NULL COMMENT '{注释}';

-- 回滚（仅在新列从未被业务写入时执行）
-- ALTER TABLE {table} DROP COLUMN {col};
```

# 4、接口设计

> 如涉及关键内部方法（领域 match / 校验 / 聚合等），可补一节「方法签名 + 逻辑要点 + 单测点」，便于直接落地与自测。

_接口公共规范，如header规范，入参规范，返回规范，验签，加密，业务状态码等_。

_接口列表汇总_

示例

| 接口名称 | 接口描述 | 调用系统 | 确认人 | 是否确认 |
| --- | --- | --- | --- | --- |
| ### order/create | 创建订单 | xyf-api-gateway |  |  |

## 4.1、交易类接口

### 4.1.1、创建订单（示例） order/create

**路由：**order/create

**method：**POST

**功能描述：**创建订单

**性能描述：**QPS<=1000时响应时间<=100ms

**限流策略：**无

**入参说明：**

| 参数名 | 中文描述 | 类型 | 是否必填 | 备注 |
| --- | --- | --- | --- | --- |
| userId | 用户ID | string | 是 |  |
| data | 创建订单数据 | object | 是 |  |

data

| 参数名 | 中文描述 | 类型 | 是否必填 | 备注 |
| --- | --- | --- | --- | --- |
| purpose | 借款用途 | string | 是 |  |
| amount | 金额 | int | 是 |  |
| period | 期数 | int | 是 |  |

**返回结果(application/json)**

| 参数名 | 中文描述 | 类型 | 是否必填 | 备注 |
| --- | --- | --- | --- | --- |
| order_no | 订单号 | string | 是 |  |

## 4.2、管理类接口

## 4.3、查询类接口

# 5、非功能性设计

## 5.1、监控

## 5.2、核对

### 5.3、联调/回归用例

# 6、其他

## 6.1、排期

_（一句话写关键节点：提测 / 联调 / 上线时间；未定写"待对齐"）_
